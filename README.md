# nano-GPT
This notebook downloads and processes the Tiny Shakespeare dataset, encodes characters into integers, builds a vocabulary, and sets up the foundation for training a character-level language model, similar to nanoGPT.
